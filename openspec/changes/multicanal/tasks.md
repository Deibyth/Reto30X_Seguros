# Tasks: Multichannel Messaging and Lightweight CRM

## Review Workload Forecast

| Field | Value |
|---|---|
| Total estimated changed lines | 4,840 additions + deletions |
| Per-slice forecast | S1 360; S2 390; S3 390; S4 390; S5 380; S6 380; S7 250; S8 350; S9 390; S10 260; S11 340; S12 390; S13 320; S14 250 |
| Suggested split | Draft tracker `feature/multicanal` → S1 → … → S14; each child targets its immediate predecessor |
| Delivery / chain | force-chained / feature-branch-chain |

Decision needed before apply: No — the approved feature-branch chain resolves the budget risk.
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High

## Work-unit contract

Each checkbox is one autonomous PR/commit. Execute RED → GREEN → REFACTOR; include tests, implementation, relevant docs/config, rollback note, exact command/results, runtime observation, and this checkbox update together. Stop and split before 400 authored changed lines. No branch targets `main`; `main`, original Compose, database, and volume remain untouched.

## Dependency-ordered slices

- [x] **S1 Isolation and guarded migrations (360).** Outcome: dedicated profile/path/volume, sentinel/identity/checksum migrator; non-goal: canonical schema. Paths: `docker-compose.multicanal.yml`, `backend/app/migrations/`, `backend/tests/test_multicanal_isolation.py`. RED hashes original bytes/schema and rejects wrong path/profile, symlink, original volume, missing sentinel before access; GREEN guard/startup; REFACTOR target policy. Verify pytest plus migrate dry run and original `/chat`; rollback removes feature-only storage. Base: tracker. Trace: data-models/versioning, docker/rollback. Evidence: hashes, schema, Compose render.
- [x] **S2 Operator security foundation (390).** Outcome: Argon2id bootstrap/login sessions, CSRF, permissions, append-only audit; non-goal: Settings UI. Paths: `backend/app/security/`, `backend/app/routers/auth.py`, `backend/tests/test_operator_auth.py`. RED unauthorized/audit-failure cases; GREEN APIs; REFACTOR dependencies. Verify pytest/login harness; rollback routers/tables. Base: S1. Trace: operator auth/audit. Evidence: status/cookie/redaction results.
- [x] **S3 Keys and vault (390).** Outcome: scoped hashed keys, rotation/revocation, AES-GCM secrets; non-goal: channel configuration. Paths: `backend/app/security_api_keys.py`, `backend/app/vault.py`, `backend/tests/test_keys_vault.py`. RED unsafe placement/scope, one-time disclosure, decrypt failure; GREEN services; REFACTOR constant-time boundaries. Verify pytest/CLI; rollback key/vault modules. Base: S2. Trace: keys/secrets. Evidence: masked persistence and denial logs. Follow-up: audit failure returns bounded 503 after rollback; rotation lineage stores the original `api_keys.id`.
- [ ] **S4 Canonical ledger (390).** Outcome: migrated contacts/identities/chats/messages/work/idempotency/attempt constraints and retention states; non-goal: worker. Paths: `backend/app/models/multichannel.py`, `backend/app/migrations/versions/`, `backend/tests/test_multichannel_models.py`. RED duplicate/reference/stale/redaction cases; GREEN schema; REFACTOR indexes. Verify pytest+migration replay; rollback latest migration on isolated DB. Base: S3. Trace: data-models, messaging addressing/dedupe. Evidence: constraint/schema results.
- [ ] **S5 Worker, routing, handoff (380).** Outcome: ordered leases/retries/dead letters, singleton worker, config snapshots, ownership fencing; non-goal: HTTP transports. Paths: `backend/app/multichannel/{worker,routing,handoff}.py`, `backend/tests/test_worker_handoff.py`. RED overtaking/stale-worker/competing-worker/takeover/transfer races; GREEN state machine; REFACTOR ports. Verify pytest/worker harness; rollback disables worker. Base: S4. Trace: messaging queue, agent-routing. Evidence: deterministic timelines.
- [ ] **S6 Webhook and reply APIs (380).** Outcome: signed SSRF-safe webhook, canonical idempotent replies/status/replay; non-goal: provider adapters. Paths: `backend/app/integrations/{webhook,replies}.py`, `backend/app/routers/integrations.py`, `backend/tests/test_integrations.py`. RED ownership race, provider-ID, DNS/redirect/rebind, body-ignore tests; GREEN APIs; REFACTOR signer/resolver. Verify pytest/fake network; rollback disables external mode. Base: S5. Trace: outbound-api, routing. Evidence: request/attempt records.
- [ ] **S7 Telegram adapter (250).** Outcome: authenticated text ingress/send/health mapping; non-goal: media. Paths: `backend/app/adapters/telegram.py`, `backend/app/routers/telegram.py`, `backend/tests/test_telegram_adapter.py`. RED bad secret/duplicate/unsupported/throttle cases; GREEN adapter; REFACTOR canonical port. Verify pytest/provider stub; rollback disables Telegram connection. Base: S6. Trace: channel-adapters. Evidence: canonical rows/outcomes.
- [ ] **S8 Baileys sidecar foundation (350).** Outcome: Node 20 authenticated ingress/send, transient QR, protected credentials, owner lock, health, isolated auth volume; non-goal: UI pairing. Paths: `sidecar/baileys/`, `docker-compose.multicanal.yml`, `backend/app/adapters/baileys.py`. RED auth/credential-before-use/logout/corruption/duplicate-owner/secret-render tests; GREEN sidecar; REFACTOR protocol. Verify sidecar tests+Compose health; rollback stops sidecar retaining volumes. Base: S7. Trace: adapters, docker. Evidence: health/restart/secret scans.
- [ ] **S9 CRM API (390).** Outcome: cursor reads and idempotent contact/customer/stage/closer/owner mutations; non-goal: UI. Paths: `backend/app/crm/`, `backend/app/routers/crm.py`, `backend/tests/test_crm_api.py`. RED uniqueness/link/stage/stale/authorization cases; GREEN APIs; REFACTOR mutation service. Verify pytest/API harness; rollback router/service. Base: S8. Trace: contact-crm. Evidence: pages/history/audit results.
- [ ] **S10 Frontend test/auth foundation (260).** Outcome: minimal Vitest+jsdom+RTL and protected auth/navigation shell; non-goal: secrets/forms. Paths: `frontend/{package.json,vite.config.ts}`, `frontend/src/test/`, `frontend/src/features/auth/`, `frontend/src/{App.tsx,components/layout/Header.tsx}`. RED unauthorized render/session-loss; GREEN harness/guard; REFACTOR containers. Verify `pnpm test --run` + build/browser login; rollback dependencies/routes. Base: S9. Trace: UI protected navigation. Evidence: test/build screenshots.
- [ ] **S11 Settings (340).** Outcome: protected APIs/UI for versioned global mode/connections and preserve/replace/delete secrets; non-goal: inbox. Paths: `backend/app/routers/settings.py`, `frontend/src/features/settings/`, matching tests. RED masked/preserve/invalid-replacement/conflict tests; GREEN backend then UI; REFACTOR typed API. Verify pytest, Vitest, browser; rollback disables routes/navigation. Base: S10. Trace: security settings, UI settings. Evidence: ciphertext/masked/conflict results.
- [ ] **S12 Inbox and human reply UI (390).** Outcome: canonical ordered inbox/history, ownership controls, optimistic lifecycle; non-goal: CRM editing. Paths: `backend/app/routers/inbox.py`, `frontend/src/features/inbox/`, matching tests. RED refresh dedupe, duplicate submit, ownership-change cases; GREEN APIs/UI; REFACTOR query containers. Verify pytest+Vitest+browser; rollback navigation/router. Base: S11. Trace: messaging, handoff, UI inbox/reply. Evidence: order/send-state captures.
- [ ] **S13 CRM UI (320).** Outcome: contact/link/stage/closer editors with deliberate conflict retry; non-goal: metrics. Paths: `frontend/src/features/crm/`, `frontend/src/lib/api.ts`, CRM component tests. RED stale-conflict/unauthorized cases; GREEN UI; REFACTOR presentation split. Verify Vitest/build/browser; rollback CRM route. Base: S12. Trace: contact-crm, UI CRM. Evidence: mutation/conflict captures.
- [ ] **S14 Metrics, legacy bridge, operations (250).** Outcome: safe partial metrics, authenticated once-only legacy bridge, retention/backup/restore/runbook; non-goal: main merge. Paths: `backend/app/{metrics,retention}/`, `backend/app/routers/{analytics,outbound}.py`, `frontend/src/features/metrics/`, `docs/multicanal-operations.md`, tests. RED partial metric, anonymous legacy, bridge-repeat, identity-mismatched restore cases; GREEN behavior; REFACTOR queries. Verify full pytest/Vitest/build, Compose rollback and `/chat`; rollback disables bridge/workers. Base: S13. Trace: UI metrics, outbound legacy, retention/docker. Evidence: full-suite, backup/restore, original-integrity results.

## First apply

Start S1 only; it establishes the fail-closed isolation boundary required by every later slice.
